export const invitationExchange=(()=>{if(window.location.pathname!=='/i')return Promise.resolve({exchanged:false});const params=new URLSearchParams(window.location.hash.slice(1));const token=params.get('t');if(window.location.hash)window.history.replaceState(null,'',window.location.pathname+window.location.search);if(!token)return Promise.resolve({exchanged:false});return fetch('/api/public/invitations/exchange',{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({token})}).then(async response=>{if(!response.ok)throw new Error('Invalid invitation access.');return{exchanged:true};});})();
