const crypto=require('crypto');const DeliveryWebhookEvent=require('../models/DeliveryWebhookEvent');const EmailMessage=require('../models/EmailMessage');const CampaignDelivery=require('../models/CampaignDelivery');
const RANK={queued:0,accepted:1,sent:2,delayed:2,delivered:3,opened:4,clicked:4,failed:5,bounced:5,complained:5};
function webhookSecret(){return String(process.env.RESEND_WEBHOOK_SECRET||'');}
function verifySignature({rawBody,id,timestamp,signature,now=Date.now()}){const secret=webhookSecret();if(!secret||!id||!timestamp||!signature||!Buffer.isBuffer(rawBody))return false;const seconds=Number(timestamp);if(!Number.isFinite(seconds)||Math.abs(now-seconds*1000)>5*60*1000)return false;let key;try{key=Buffer.from(secret.startsWith('whsec_')?secret.slice(6):secret,'base64');}catch{return false;}const expected=crypto.createHmac('sha256',key).update(`${id}.${timestamp}.`).update(rawBody).digest('base64');return String(signature).split(' ').some(part=>{const value=part.startsWith('v1,')?part.slice(3):'';const a=Buffer.from(value),b=Buffer.from(expected);return a.length===b.length&&a.length>0&&crypto.timingSafeEqual(a,b);});}
function mapping(type,at){const providerStatus=String(type||'').replace('email.','');const status=providerStatus==='delivery_delayed'?'delayed':providerStatus;const fields={status,providerStatus,lastEventAt:at};if(status==='sent')fields.sentAt=at;if(status==='delivered')fields.deliveredAt=at;if(status==='opened')fields.openedAt=at;if(status==='clicked')fields.clickedAt=at;if(status==='delayed')fields.delayedAt=at;if(status==='bounced')fields.bouncedAt=at;if(status==='complained')fields.complainedAt=at;if(status==='failed')fields.failedAt=at;return{status,fields};}
async function processEvent(payload,eventId){let event;try{event=await DeliveryWebhookEvent.create({provider:'resend',eventId,eventType:payload.type,providerMessageId:payload.data?.email_id||null});}catch(e){if(e.code===11000)return{duplicate:true};throw e;}const providerMessageId=payload.data?.email_id;const message=providerMessageId&&await EmailMessage.findOne({providerMessageId});if(!message){event.processedAt=new Date();event.matched=false;await event.save();return{matched:false};}const at=payload.created_at?new Date(payload.created_at):new Date();const {status,fields}=mapping(payload.type,at);if(!(status in RANK)){event.processedAt=new Date();event.matched=true;await event.save();return{matched:true,ignored:true};}if((RANK[message.status]||0)<=RANK[status]){Object.assign(message,fields);await message.save();const deliverySet={providerMetadata:{lastResendEvent:payload.type,lastEventAt:at}};if(status==='sent')Object.assign(deliverySet,{status:'sent',sentAt:message.sentAt,retryEligible:false});if(status==='delivered')Object.assign(deliverySet,{status:'delivered',deliveredAt:at,retryEligible:false});if(status==='opened')Object.assign(deliverySet,{status:'opened',openedAt:at,retryEligible:false});if(['failed','bounced','complained'].includes(status))Object.assign(deliverySet,{status,failedAt:at,retryEligible:false,providerErrorCategory:status,providerErrorCode:String(payload.data?.bounce?.message||payload.data?.reason||status).slice(0,100)});if(status==='delayed')Object.assign(deliverySet,{providerMetadata:{lastResendEvent:payload.type,lastEventAt:at,delayed:true}});await CampaignDelivery.updateOne({_id:message.deliveryId},{$set:deliverySet});}event.processedAt=new Date();event.matched=true;await event.save();return{matched:true};}
module.exports={RANK,verifySignature,mapping,processEvent};
