const express=require('express');const{verifySignature,processEvent}=require('../../services/resendWebhookService');const router=express.Router();
router.post('/',async(req,res,next)=>{try{const raw=req.body;if(!verifySignature({rawBody:raw,id:req.get('svix-id'),timestamp:req.get('svix-timestamp'),signature:req.get('svix-signature')}))return res.status(401).json({error:'Invalid webhook signature.'});let payload;try{payload=JSON.parse(raw.toString('utf8'));}catch{return res.status(400).json({error:'Invalid webhook payload.'});}await processEvent(payload,req.get('svix-id'));return res.json({success:true});}catch(e){next(e);}});module.exports=router;
